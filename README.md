## SEKAWAN Lab

1. Clone the project
2. Run `composer dump-autoload`
3. Config your database credential on file `config/database.php`
4. Run `php .\database\setup.php`
5. Run `php -S localhost:8000 -t public`
6. Visit `localhost:8000` on your local browser
